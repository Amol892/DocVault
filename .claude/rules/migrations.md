---
paths:
  - "backend/alembic/**"
  - "backend/app/models/**"
---

# Migrations & models

- Model change ⇒ new Alembic revision in the same commit. Review autogenerated output; autogenerate misses RLS policies, enums changes and data migrations.
- Never edit a revision that has been merged; write a new one.
- Every migration must be reversible (`downgrade`) unless the PRD explicitly says otherwise.
- Schema must match `PRD/04-database-schema.md`. Flag any divergence instead of quietly changing it.
- Add indexes for every foreign key and every `workspace_id` filter path.
